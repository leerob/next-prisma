# Migration `20200916120542-asdasd`

This migration has been generated by William Luke at 9/16/2020, 3:05:42 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Song" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "name" TEXT NOT NULL,
    "test" TEXT NOT NULL DEFAULT 'Test',
    "testing" TEXT NOT NULL DEFAULT 'Testing',
    "human" TEXT NOT NULL DEFAULT 'Human',
    "nomu" TEXT NOT NULL DEFAULT 'Human',
    "nomuuuuu" TEXT NOT NULL DEFAULT 'Human',
    "youtubeId" TEXT,
    "albumCoverUrl" TEXT,
    "artistId" INTEGER,
    "playlistId" INTEGER,

    FOREIGN KEY ("artistId") REFERENCES "Artist"("id") ON DELETE SET NULL ON UPDATE CASCADE,
    FOREIGN KEY ("playlistId") REFERENCES "Playlist"("id") ON DELETE SET NULL ON UPDATE CASCADE
);
INSERT INTO "new_Song" ("id", "name", "youtubeId", "albumCoverUrl", "artistId", "playlistId", "test", "testing", "human", "nomu") SELECT "id", "name", "youtubeId", "albumCoverUrl", "artistId", "playlistId", "test", "testing", "human", "nomu" FROM "Song";
DROP TABLE "Song";
ALTER TABLE "new_Song" RENAME TO "Song";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200916115813-asfg..20200916120542-asdasd
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -13,9 +13,9 @@
   test    String @default("Test")
   testing String @default("Testing")
   human   String @default("Human")
   nomu    String @default("Human")
-
+  nomuuuuu    String @default("Human")
   youtubeId     String?
   albumCoverUrl String?
   artist        Artist?   @relation(fields: [artistId], references: [id])
   artistId      Int?
```


